import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'aidilrx04.duitGone',
	appName: 'duitGone',
	webDir: 'build',
	server: {
		androidScheme: 'https'
	}
};

export default config;
